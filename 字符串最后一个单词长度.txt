#字符串最后一个单词的长度

import java.util.*;
public class Main{
    public static void main(String[] args)
    {
        Scanner scanner = new Scanner(System.in);
        //1.使用split函数将字符串分为字符串数组, 将每个单词作为一个元素
        //获得最后一个的单词的长度即可
        //2.从尾部开始遍历, 遇到第一个空格结束
    }
}